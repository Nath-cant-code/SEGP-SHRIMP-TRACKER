# Copy this file to .env and fill in the values

# SQLite (recommended for local dev — just use the path below as-is)
DATABASE_URL=sqlite:///./shrimp_tracker.db

# The origin your React dev server runs on.
# Vite default is 5173. Change if you use a different port.
FRONTEND_ORIGIN=http://localhost:5173
